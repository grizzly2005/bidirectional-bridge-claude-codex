interface:
  display_name: "Using Bridge"
  short_description: "Coordinate bounded work between Claude Code and Codex"
  default_prompt: "Use the repository bridge only when bounded delegation, recovery, or telemetry is materially useful."
policy:
  allow_implicit_invocation: true
